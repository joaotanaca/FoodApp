import global from "./global";
import { mixinsStyled } from "./mixins";
import themeColor from "./theme";

export const theme = themeColor;
export const Global = global;
export const mixins = mixinsStyled;
